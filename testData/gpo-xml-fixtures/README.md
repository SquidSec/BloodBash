# GPO XML fixtures (GPP / cPassword)

Synthetic SYSVOL-style layout for `--gpo-content-dir` tests.

```bash
python3 BloodBash.py testData/synthetic-corp-lab \
  --gpo-content-dir testData/gpo-xml-fixtures --gpo-parsing
```

No real domain data.
